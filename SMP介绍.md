# Skills Manager Pro 介绍

Skills Manager Pro，简称 SMP，是一款面向 Codex、Claude 与 WorkBuddy 的本地 Skill 管理工作台。

当 Skill 越装越多，真正麻烦的往往不是“能不能调用”，而是你很难回答这些问题：某个 Skill 到底装在哪里？哪些可以修改？哪些来自插件缓存？它有没有风险？最近改过什么？如果要整理、重命名或恢复，怎样才能不误伤原文件？

SMP 就是为这类日常管理问题设计的。它把散落在不同宿主、不同目录和不同来源中的 Skill 统一索引到一个本地界面里，让查看、整理、编辑、审计和恢复都有清晰的操作边界。

## SMP 能做什么

### 1. 统一管理多个 Skill 宿主

SMP 可以自动发现并索引 Codex、Claude 和 WorkBuddy 的用户 Skill、项目 Skill、插件缓存、市场目录以及其他自定义扫描目录。

你可以按宿主、来源、可编辑状态、启用状态和回收站状态筛选 Skill，也可以直接搜索名称、显示名和说明。不同来源会被明确标记，避免把用户文件、插件文件、缓存文件和市场文件混在一起处理。

### 2. 查看完整 Skill 结构

除了 `SKILL.md`，SMP 还会展示 Skill 目录中的其他文本资源，例如：

- `references` 参考资料
- `agents` 或宿主相关元数据
- 脚本与其他附属文件
- 文件大小、更新时间、编码和可编辑状态

你可以在工作台中浏览这些内容，了解一个 Skill 的完整组成，而不必反复打开资源管理器、编辑器和多个配置目录。

### 3. 安全编辑 Markdown 与元数据

对于用户来源和项目来源的可编辑 Skill，SMP 支持直接修改：

- YAML frontmatter 元数据
- Markdown 正文
- `references` 等文本资源
- Codex UI 元数据和其他已识别文本文件

编辑器会尽量保留未知字段、原有注释、换行格式和其他宿主扩展字段。每次保存都会创建可恢复快照，方便在修改后回看差异或恢复此前版本。

显示名和内部名称也可以分开处理。修改内部名称时，SMP 会预检目录、已知调用名称、Codex UI 元数据和自引用，减少改名后出现调用失效的可能。

### 4. 本地诊断与 Skill 审计

SMP 不只展示文件，还会对 Skill 做本地结构检查和风险提示，包括：

- 元数据是否缺失或格式异常
- Skill 是否使用宿主的旧式字段
- 是否包含脚本、二进制文件或外部链接
- 引用路径是否超出 Skill 目录
- 是否存在疑似符号链接越界
- 文件是否过大或处于受保护来源
- 当前 Skill 是否适合继续编辑

这些检查帮助你在使用前先了解 Skill 的结构、来源和风险。脚本只会被盘点，不会被 SMP 自动执行。

### 5. 操作历史与差异查看

SMP 会记录文本编辑、资源修改、显示名修改、内部改名、分类标签、回收、恢复和 AI 分析等操作。

历史记录包含操作时间、文件路径、前后哈希、操作摘要和可用的文本差异。对于有快照的修改，可以直接恢复此前版本。你不需要依靠记忆判断“刚才改了什么”，也不必在多个备份目录中寻找旧文件。

### 6. 工作台分类、标签与个人备注

分类和标签只用于 SMP 内部的整理，不会移动 Skill，也不会改变宿主的调用路径。

你还可以为每个 Skill 添加 Markdown 使用备注，并插入本地图片。备注保存在工作台自己的数据库中，不会改写、移动或污染原 Skill 文件，适合记录调用方式、适用场景、待修复问题和个人经验。

### 7. AI 辅助分析

配置 AI 服务后，SMP 可以对 Skill 进行结构化分析，帮助提炼：

- Skill 的功能摘要
- 主要能力
- 推荐分类和标签
- 触发描述是否清晰
- 宿主兼容性提示
- 风险标记
- 可执行的改进建议

也支持批量 AI 分析。不过 AI 只提供分析结果，不会擅自执行 Skill 中的指令，也不会替你自动修改文件。涉及文件变化的操作仍然由用户确认。

AI 服务配置使用系统安全存储加密保存，SMP 会拒绝在系统安全存储不可用时保存明文密钥。

### 8. 回收站与恢复

对可管理的用户来源和项目来源 Skill，SMP 提供工作台回收站。移入回收站前会记录原路径和哈希，之后可以恢复到原位置。

插件、内置、市场、缓存和备份来源默认保持只读。它们可以被查看、索引和诊断，但不会被误当成普通用户文件修改或删除。

## SMP 做过哪些优化

SMP 的优化重点一直围绕“看得清、改得稳、退得回”展开。

### 从分散目录到统一索引

早期的 Skill 管理通常依赖手动查目录。SMP 增加了多宿主自动发现、来源识别、插件归属识别和自定义扫描根，让 Skill 进入一个统一的本地索引。

### 从简单文本编辑到结构化编辑

编辑 Skill 不能只做字符串替换。SMP 增加了 Markdown、YAML 和高级资源编辑，并专门处理未知字段、注释、宿主扩展字段、长内容滚动和不同换行格式，降低编辑过程中破坏原文档结构的风险。

### 从“改了就算”到可追溯操作

保存文本前创建快照，历史页展示前后哈希和差异，改名、回收、恢复、分类和 AI 分析分别留下记录。这样每一次变化都有来源，也有回退路径。

### 从“所有文件都能改”到明确权限边界

SMP 将用户、项目、插件、内置、市场、缓存、备份和回收站区分开来。可编辑来源与受保护来源在界面中明确标记，脚本和受保护资源保持只读，避免为了方便管理而越过文件本身的安全边界。

### 从“能启动”到稳定的本地应用体验

SMP 还持续优化了 Windows 安装启动、应用加载、目录监控、窄窗口布局、长文本编辑、搜索快捷键和侧栏筛选等细节，让它更适合长期作为本地工具使用。

### 从单一界面到中英文与品牌统一

0.2.0 增加了“跟随系统 / 简体中文 / English”语言设置，并将 AI 服务入口整合到软件设置中。0.2.1 进一步统一了 Skills Manager Pro 的中英文名称、运行时窗口、安装包和压缩包名称，同时保留原有数据目录和内部升级标识，避免已有索引、设置与历史记录失联。

## SMP 和其他工具有什么区别

### 它不是单纯的 Skill 安装器

安装器解决的是“把 Skill 放进来”。SMP 关注的是安装之后的长期管理，包括来源识别、结构查看、编辑、诊断、历史和恢复。

### 它不是只展示名称的文件列表

SMP 会进入 Skill 的文件结构，识别元数据、参考资料、脚本、外部链接和路径关系，并把可编辑性、来源和诊断结果放到同一个管理界面中。

### 它不是只依赖人工经验的审计表

SMP 将本地结构诊断、风险提示、操作历史和 AI 分析结合起来。AI 可以帮助理解内容，但关键操作仍保留用户确认和本地可追溯记录。

### 它不是会替你执行未知脚本的自动化工具

SMP 的基本原则是先盘点、再判断、后操作。脚本只做检查，不会被工作台自动运行；插件和缓存默认只读；删除操作进入工作台回收站。

### 它不是把文件强行迁移到统一目录的导入器

SMP 直接扫描现有目录，不要求把 Skill 搬家，也不会为了建立索引而改变宿主的目录结构。工作台中的分类、标签和备注属于本地管理信息，与原 Skill 文件保持分离。

## 适合哪些人

- 同时使用 Codex、Claude、WorkBuddy 等多个 AI 宿主的人
- 已经积累了较多个人 Skill、项目 Skill 或插件 Skill 的开发者
- 需要检查 Skill 来源、结构和风险的团队或个人
- 经常修改 Markdown、YAML、references 和宿主元数据的人
- 希望保留修改历史，不想误删或误改本地 Skill 的用户
- 想用 AI 辅助整理 Skill，但仍希望自己掌握最终决定权的人

## 一句话理解 SMP

Skills Manager Pro 不是一个把 Skill 装进电脑的工具，而是一个帮助你看懂、整理、审计、修改和安全维护本地 Skill 资产的工作台。

当前版本：0.2.1
