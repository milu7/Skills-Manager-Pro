# v0.1.0 验收记录

验收日期：2026-08-13

## 自动验证

- TypeScript 严格检查通过。
- Vitest：6 个测试文件、26 项测试全部通过，覆盖三平台元数据、Claude 扩展字段、WorkBuddy 旧格式、插件清单、BOM/CRLF、损坏 YAML、重复家族、原子保存冲突、内部改名、回收恢复、双 AI 协议、Schema 降级、鉴权脱敏、限流、超时、无效 JSON 与取消。
- Playwright Electron E2E 通过：首启引导、三平台/四个夹具索引、渲染进程 Node 隔离、AI 服务加密保存、4 项批量发送清单、编辑差异、历史快照和恢复。
- 打包版冒烟通过：主进程、沙箱渲染进程、SQLite 数据库和 `better-sqlite3` 原生模块均正常；测试结束后无打包版进程残留。
- `npm audit --omit=dev`：运行时依赖 0 个已知漏洞。

## 当前机器真实扫描

隔离数据库只读扫描 847 个当前可见安装副本，耗时 13.897 秒，达到完整扫描不超过 15 秒的目标。结果包括：

- 用户来源 53
- 市场候选 697
- 缓存 41
- 备份 4
- 内置 52
- 本地诊断：80 个警告、26 个错误、228 个重复项

数量来自验收时的磁盘快照，不写入代码，也不作为未来恒定值。

## 分发产物

| 产物 | 大小 | SHA-256 |
| --- | ---: | --- |
| `Skill-Workbench-Setup.exe` | 141,392,384 B | `618A699B6AADAA940223D1DDA2B4782D24E048ED22A93485882E5A3260A4072E` |
| `Skill 管理工作台-win32-x64-0.1.0.zip` | 145,896,500 B | `AA5501FA6E2976CA38E26CC37AA8B24390D52C585F948DC5687334EF5E72ED0A` |
| `skill_workbench-0.1.0-full.nupkg` | 140,639,585 B | `0D27CE7AD4215567C8341E0EABDEC209C401755F0642B59206A9A1591A0D6AF6` |

## 发布边界

- 安装器与主程序当前未做 Authenticode 代码签名。对外分发前应购买或配置证书、签名并重新记录哈希。
- 完整 `npm audit` 仍报告 28 项开发/打包工具链告警，主要来自 Electron Forge 的 `@electron/node-gyp`、`tar`、`tmp` 和开发服务器传递依赖；这些依赖不进入生产运行时，强制跨主版本覆盖可能破坏 Forge 构建，因此 v0.1.0 没有盲目覆盖。升级 Forge 后应重新审计。
- 真实宿主中的触发效果、第三方 AI 服务兼容性和已签名安装流程需要在对应环境单独验收；本记录不把本地夹具验证描述为线上验证。
